package com.strydeeva.repository;

import com.strydeeva.entity.CustomSize;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CustomSizeRepository extends JpaRepository<CustomSize, Long> {
}
